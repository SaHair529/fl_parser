# FL.ru amoCRM парсер

Парсит **все страницы** раздела "Программирование" на FL.ru каждые 5 минут, ищет упоминания **amoCRM**, **Bitrix24**, **1C** и отправляет новые заявки в Telegram.

## Что делает:
- Проверяет все страницы проектов (`?kind=1&page=N`)
- Ищет ключевые слова: `amocrm`, `amo crm`, `amo-crm`, `bitrix24`, `1c`, `битрикс`
- Сохраняет проверенные ссылки в `flru_last_projects.json` (чтобы не спамить)
- Отправляет только **новые** проекты в твой Telegram
- Логирует каждый шаг в консоль/cron лог

## Стек:
- Python 3.11+
- requests - HTTP запросы
- beautifulsoup4 - парсинг HTML
- re - поиск по регуляркам
- json - сохранение состояния
- datetime - логирование времени

## Ключевые фичи:
- ✅ Парсит **ВСЕ страницы** до пустой
- ✅ Автоматически режет длинные сообщения для Telegram (лимит 4096 символов)
- ✅ Задержки между страницами (1 сек) - антибан
- ✅ User-Agent ротация (имитация браузера Chrome)
- ✅ Обработка ошибок (сети, HTML изменений)
- ✅ Разовый запуск для cron (без бесконечного цикла)

## Логика поиска:
1. Загружает старые ссылки из JSON
2. Парсит каждую страницу → находит блоки `data-id="qa-lenta-*"` или `b-post`
3. Извлекает заголовок (`h2.b-post__title a`) + описание (`b-post__txt`, `text-5`)
4. Проверяет регуляркой `r'amocrm|amo crm|amo-crm|bitrix24?|1c|amo|битрикс'`
5. Новые → в Telegram, все → в JSON

## Telegram:
📋 Интеграция amoCRM с 1С
🔗 Ссылка (стр. 2)
💬 Требуется разработка API интеграции amoCRM ↔ 1С...
⏰ 09:35 03.01.2026
───────────────

Запускается по крону